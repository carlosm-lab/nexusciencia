# ==========================================================================
#  NEXUS CIENCIA - VARIABLES DE ENTORNO (PLANTILLA)
# ==========================================================================
#  INSTRUCCIONES:
#  1. Copia este archivo como .env (sin el .example)
#  2. Configura todos los valores según tu ambiente
#  3. NUNCA subas el archivo .env a Git (está en .gitignore)
# ==========================================================================
#
#  ⚠️  ADVERTENCIA DE SEGURIDAD ⚠️
#  - NUNCA compartas tus credenciales reales
#  - Si crees que fueron expuestas, ROTALAS inmediatamente:
#    * SECRET_KEY: Genera una nueva (ver comando abajo)
#    * GOOGLE_CLIENT_SECRET: Regenera en Google Cloud Console
#  - El archivo .env NUNCA debe estar en control de versiones
#
# ==========================================================================

# --- SEGURIDAD ---
# IMPORTANTE: La SECRET_KEY debe tener MÍNIMO 32 caracteres hexadecimales (64 chars)
# Genera una clave secreta segura con:
#   python -c "import secrets; print(secrets.token_hex(32))"
# 
# Ejemplo de clave válida (NO uses esta, genera la tuya):
#   a3f8b2c1d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1
#
SECRET_KEY=GENERA_UNA_CLAVE_SEGURA_CON_EL_COMANDO_DE_ARRIBA

# --- SESIÓN Y AUTENTICACIÓN ---
# Timeout de inactividad de sesión en minutos (default: 30)
SESSION_INACTIVITY_MINUTES=30

# --- GOOGLE OAUTH ---
# Obtén las credenciales en: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# --- ADMINISTRACIÓN ---
# Email del administrador (debe coincidir con cuenta de Google)
ADMIN_EMAIL=admin@example.com

# --- BASE DE DATOS ---
# Para desarrollo local (SQLite): Comenta o deja vacío
# Para producción (MySQL): Descomenta y configura
# DATABASE_URL=mysql+pymysql://usuario:contraseña@localhost/nexusciencia

# --- AMBIENTE ---
# Opciones: development, production, testing
FLASK_ENV=development

# --- RATE LIMITING (OPCIONAL) ---
# Activar rate limiting en desarrollo (por defecto: False)
# RATELIMIT_ENABLED=True

# --- DOCKER / MYSQL (Requerido para docker-compose) ---
# Estas variables son OBLIGATORIAS si usas docker-compose
# No tienen valores por defecto por seguridad
MYSQL_ROOT_PASSWORD=cambia_esta_password_root
MYSQL_DATABASE=nexusciencia
MYSQL_USER=nexus
MYSQL_PASSWORD=cambia_esta_password_user

# --- REDIS (Opcional para rate limiting avanzado) ---
# REDIS_URL=redis://localhost:6379/0

# --- SENTRY (Opcional para monitoreo en producción) ---
# SENTRY_DSN=https://your-sentry-dsn
# RELEASE_VERSION=v3.1.0
